# Ambiente de execução
NODE_ENV=production

# Banco de dados PostgreSQL (via Docker)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=2004npmiPgOl
DB_NAME=kanban_db

# JWT - Chave secreta para tokens
JWT_SECRET=sua_chave_secreta_super_segura_aqui_mude_em_producao

# Servidor
PORT=3000

# CORS
CORS_ORIGIN=*
